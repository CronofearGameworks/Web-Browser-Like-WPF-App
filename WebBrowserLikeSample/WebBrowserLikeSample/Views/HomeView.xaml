<UserControl x:Class="WebBrowserLikeSample.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebBrowserLikeSample.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" Width="250">
            <Button Command="{Binding OpenView1}" Content="View1"/>
            <Button Command="{Binding OpenView2}">View 2</Button>
            <Button Command="{Binding OpenView3}">View 3</Button>
        </StackPanel>
    </Grid>
</UserControl>
